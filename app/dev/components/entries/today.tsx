import type { Entry } from '../types'

/** `/dev/components` entries of features/today (task 5.1b; 5.2b and 5.4 later) — Part B-M5 decision 3: only that task edits this file. */
export const TODAY_ENTRIES: Entry[] = []
