import type { Entry } from '../types'

/** `/dev/components` entries of "Học thêm", track progress and reset (task 5.4) — Part B-M5 decision 3: only that task edits this file. */
export const EXTRA_ENTRIES: Entry[] = []
